{
  "name": "vocabularies-learn",
  "private": true,
  "packageManager": "npm@11.19.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "web": "npm start -w apps/web",
    "api": "npm run start:dev -w apps/api",
    "build:web": "npm run build -w apps/web",
    "build:api": "npm run build -w apps/api"
  }
}
