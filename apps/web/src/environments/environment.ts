// Production settings — used by `ng build` (the default production build).
export const environment = {
  production: true,
  // TODO: set to your deployed NestJS API URL once it is hosted.
  apiUrl: 'http://localhost:3000',
};
