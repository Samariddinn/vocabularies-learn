// Development settings — swapped in for environment.ts by `ng serve`
// (see fileReplacements in angular.json).
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000',
};
